import Config

import_config "#{config_env()}.exs"

# You can set a global attribute namespace like this:
# config :o11y, :attribute_namespace, "app"

# You can set a global list of filtered attributes like this:
# config :o11y, :filtered_attributes, ["password", "secret", "token", "email"]
